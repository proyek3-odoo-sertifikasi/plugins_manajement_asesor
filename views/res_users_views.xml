<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        Tambahkan field Skema Sertifikasi ke form User Odoo.
        Field ini muncul untuk semua user agar Admin Sistem dapat mengisinya
        saat menetapkan role Asesor LSP.
    -->
    <record id="view_users_form_inherit_lsp_skema" model="ir.ui.view">
        <field name="name">res.users.form.inherit.lsp.skema</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="after">
                <field name="skema_sertifikasi_id"
                       string="Skema Sertifikasi (Asesor LSP)"
                       widget="many2one"
                       options="{'no_create': True}"
                       help="Isi hanya untuk user yang berperan sebagai Asesor LSP. Menentukan skema sertifikasi mana yang boleh diakses asesor ini."
                />
            </xpath>
        </field>
    </record>
</odoo>

